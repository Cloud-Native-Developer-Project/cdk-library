# Server Configuration
PORT=8080
HOST=0.0.0.0

# SFTP Configuration
SFTP_HOST=sftp
SFTP_PORT=22
SFTP_USER=addiuser
SFTP_PASSWORD=addipass
SFTP_UPLOAD_DIR=/uploads

# AWS Configuration
AWS_REGION=us-east-1

# ngrok Configuration (optional)
# Get your authtoken from: https://dashboard.ngrok.com/get-started/your-authtoken
# Without authtoken, ngrok will use free tier with random URLs
NGROK_AUTHTOKEN=

# Optional: For local testing without Docker
# SFTP_HOST=localhost
# SFTP_PORT=2222
